# Run these commands from the repository root (after starting Docker):
cd Fetch_Rewards
docker build -t coordinate-grid-demo .
docker run --rm -p 5000:5000 coordinate-grid-demo
# Open http://localhost:5000, then stop with Ctrl+C.
